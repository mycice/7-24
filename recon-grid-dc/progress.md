# Progress Log — DC Cutting (SurgicalSim_DC)

## Stage 1 (recon + physics + deformation) — COMPLETE & verified
1A static DC recon ✅, 1B-i mass-spring/RK45 ✅, 1B-ii deformation coupling ✅. All subagent-reviewed.
Real bugs caught by GPU run: IsectGpu stride 32→40; QEF D5 off-by-one; **D3 integrator sign (Fext-Fint)→(Fext+Fint)** (the actual restoring force convention — missed by all static audits, caught by GPU oracle). Jitter/no-sag diagnosed = paper-stiff ks too stiff (sub-pixel sag) + per-frame D6 h-controller hunting near stability edge; fixed with `hMax` substep cap + soft demo physics in DemoSceneSetup. User confirmed visible sag.
Full per-cluster trail in `.superpowers/sdd/progress.md`.

## Stage 2 (cutting) — STARTED 2026-06-27
Session: set up planning files (task_plan/findings/progress) in SurgicalSim_DC; redirected stale MC2024 task_plan in old Simulation repo.
Sub-stages 2A (CONN4096 LUT) → 2B (cut detection M-T) → 2C (cut surface) → 2D (dynamic+sever+ablation).
NEXT: write Stage-2 implementation design doc → multi-subagent audit (paper §2.1 + design §5 alignment) → implement 2A.

## Native CUDA port (separate track) — spec `docs/superpowers/specs/2026-06-30-liver-native-cuda-framework-design.md`
Realizing paper Fig 3.1 as a native C++/CUDA Unity plugin (`cuda_plugin/` → LiverCudaSim.dll); Unity only renders. User builds the DLL.
- 2026-06-30: **Native Stage 0/1/2 COMPLETE & user-validated in Unity** (toolchain → static DC recon → RK45 mass-spring). Pushed origin/feat/liver-native-cuda.
- 2026-06-30: **Native Stage 3 (cutting) CODE COMPLETE + AUDIT CLEAN** (awaiting user DLL build + Unity cut-gate test). Ported §2.1 verbatim from the GPU compute path:
  `cuda/cut.cu` (9 kernels: k_DetectCut M-T rest-lattice Eq6-8 + EmitCutPoint, k_SeverLinks, k_ClearCutFP, k_LookupConnectivity, k_AccumulateCutFP, k_ComputeComponentFP centroid, k_InterpCutFP EMA, k_BuildCutTriangles + EmitCutQuad, k_NormalizeCutFP), `cut.cuh` (CutPointGpu 32B / Conn4096Gpu 36B / GridEdgeGpu2 16B / RotGpu 36B, MollerTrumbore, mulRv/mulRtv, POS_SCALE), `cut.h` (CutInitDesc/CutToolDesc). `dc_recon.cu` made cut-aware (StitchVertId bit31 tag, FetchPos, k_NormalsScatter tag-routed, k_ExpandVertices non-indexed soup, interleaved recon_build_static matching DualContouring.Build order, recon_set_cut_buffers + g_cutActive gate). `physics.cu` split physics_compute_rotations out of step (runs post-sever). `plugin_api.cpp` LCS_InitCut/SetTool/DetectCut/Finalize. `LiverCudaManager.cs` marshals Conn4096(36B)/gridEdges(16B)/occupancy, keyboard rod (WASD/QE move, arrows rotate) → per-frame LCS_Step → rod substeps {Advance, SetTool, DetectCut} → Finalize → non-indexed render.
  **AUDIT: 2 rounds, 12 subagents, ~947k tok, ZERO confirmed bugs.** Round 1 (workflow wf_cab9cd92) = 8 dimension auditors (M-T, sever+ordering, Conn4096, cut-FP chain, rest-frame R, cut walls, dc integration, C# marshal) vs reference C#/HLSL + paper §2.1 → all clean (each 7-18 tool calls, file-grounded). Round 2 (workflow wf_601013a6) = 4 invariant PROOFS on the classic silent-bug sites → all hold at HIGH confidence, no cross-check needed: INV1 matrix round-trip world=ownerPos+R·(Rᵀ·(P_side−ownerPos)) byte-equivalent to reference HLSL mul() (verified via 90°-about-z numeric example); INV2 Conn4096 12-bit config index + 8-corner numbering identical to ConnectivityLUT.cs; INV3 cross-TU struct byte layouts identical (RotGpu 36B physics↔cut, recon_set_cut_buffers pointer types, BendPairGpu 32B, nbrIdx stride, Conn4096Gpu CUDA↔C#); INV4 end-to-end data flow = no read-before-write, cutActive gate consistent, dispatch order matches DualContouring.Build (DC_WriteIndirectArgs → k_ExpandVertices).
  NEXT: user builds DLL (`cd cuda_plugin && cmake -B build -A x64 && cmake --build build --config Release` → copy LiverCudaSim.dll → Assets/Plugins/x86_64/) → Play → drag rod through liver → confirm a visible cut opens → then commit Stage 3 + push.
- 2026-06-30: **Native Stage 3 — cut OFFSET fixed (deformed-edge detection) + STRICT paper-architecture audit (0 misalignments)**.
  BUG: user saw the cut land offset from the rod (the liver now deforms a lot). ROOT CAUSE (systematic-debugging): `k_DetectCut` ran M-T on the REST lattice (a declared deviation, justified only when rest≈deformed) while Unity renders the DEFORMED mesh → cut landed on tissue whose rest-pos met the rod but whose displayed pos had sagged. FIX (cut.cu:111-137): detect on the DEFORMED edge (`O=cornerPos[idA]`, `Dvec=cornerPos[idB]-O`) per paper SS2.1.3 ("M-T collision between the cutting plane and the [deformed] mesh"; SS2.1.2 "voxel grid distorted, no longer a uniform hexahedron"). Swept plane (prev→cur) + manager sub-stepping = the paper's own fast-motion safeguard, so the rest deviation is unneeded. Compile fix: added `operator-(int3,int3)` to common.cuh:51 (2-subagent compile audit → clean).
  AUDIT (workflow wf_0781e851, 15 agents, ~785k tok, excl Ablation SS2.1.4): **0 misalignments / 7 justified-deviations**; SS2.1.2 dynamic-frame fully CLEAN. The cutting ALGORITHM architecture + continuous-cut-path are STRICTLY paper-aligned. Justified-deviations: (1) k_ComputeComponentFP isectMean fallback for a cut+occupied component with 0 cut points = paper-silent degenerate case; (2) swept quad spans 1 sub-step not the literal whole-frame plane, but `SubStepCount=ceil(disp/L)` + cumulative voxelCutMask cover the race more robustly than the paper's single persisting plane; (3) `t_ray=min(t1,t2)`+clamp combine logic when an edge pierces both swept triangles = paper-silent, geometrically correct; (4) **the 3-thread + CUDA-stream concurrency of Fig 3.1/3.2 does NOT exist yet — runs single-threaded sequential (logical box ORDER + data-flow match Fig 3.1; concurrency = STAGED Stage 4 threading/zero-copy)**; (5) **no force-feedback/haptic thread (Fig 3.1 3rd thread + proxy collision) — STAGED Stage 5**; (6) physics fixed `physicsDt=0.02` (paper line 1148 mandates 0.02s) vs rod `Time.deltaTime` — decoupled but SubStepCount keeps the cut continuous; (7) NCut = swept-quad normal + per-particle ±D/2 side test = paper-silent (paper gives D thickness→2 widening points but no n_cut DIRECTION). NO code fix needed from the audit (0 misalignments). FULL computing-framework alignment (3-thread concurrency + haptic) = Stage 4/5 (always planned next).
  NEXT: user rebuilds (`cd cuda_plugin && cmake --build build --config Release` → copy DLL) → verify the cut now tracks the rod on the displayed tissue → commit Stage 3 + push → then Stage 4 (threading/zero-copy concurrency = the rest of Fig 3.1's framework).

### Session entries
- 2026-06-27: Stage 2 kickoff. Planning files created. Writing Stage-2 design doc.
- 2026-06-27: Stage 2 design audited (5-dim workflow + skeptic): **0 critical / 0 important / 10 minor / 46 confirm-ok** -> design paper-aligned & approved. Minors folded into design §10 (per-sub-stage). Path finding was false (Assets/ReconGridDC IS as-built). NEXT: write 2A (CONN4096 LUT) detailed plan -> implement -> review.
- 2026-06-27: **2A (CONN4096 LUT) COMPLETE & reviewed** (commit fa2f36f; 2 reviewers R1 opus algorithm + R2 sonnet buffer/regression). R1 hand-verified Fig 2.4 + brute-forced all 4096 vs independent reference (0 mismatch); union-on-uncut(bit==0) confirmed; vertToComp dense; no Stage-1 regression. Fixed 2 LOW test nits (dead array; test-5 injectivity guard). ConnectivityLUT.cs + conn4096Buf + 5 CPU tests (run anywhere). NEXT: 2B (M-T cut detection).
- 2026-06-27: **2B (M-T cut detection + cut points) COMPLETE & reviewed** (2 reviewers R1 opus algorithm + R2 sonnet GPU-realization). R1: Möller-Trumbore verified EXACT line-by-line vs Eq6-8 (T_vec=V0-O, leading minus on u, finite-edge t∈[0,1]); cut-bit stencil byte-exact mirror of GridConventions.EdgeStencil; ray=voxel-edge/tri=swept-plane; ±D/2 sign-by-side correct; oracles catch sign/stencil errors. **Approved.** R2 found 4 GPU-realization issues, all fixed:
  - **#1 IMPORTANT (idempotency)**: DetectCut runs every frame + VoxelCutMask persists → was re-appending the same edge's 2 cut points every frame (unbounded growth + duplicate-polluted 2C centroids). FIX: emit cut points ONLY on uncut→cut transition via `InterlockedOr` old-value of first in-bounds voxel bit. CutPoint/CutPointCounter now cumulative (NOT reset per frame). Added a 2nd-dispatch idempotency assertion to the oracle test.
  - **#2 IMPORTANT (OOB)**: no capacity guard on `_CutPoint[slot]` write. FIX: added `_CutPointCapacity` uniform (set by CutDetector + test) + `if(slot>=cap) return` guard.
  - **#3 MODERATE (alignment)**: HLSL `GridEdgeGpu2` used `int3 baseCorner` — deviated from proven Stage-1 `GridEdgeGpu` HLSL layout (3 explicit ints). FIX: split to `int bcX,bcY,bcZ` (offsets 0/4/8/12, unambiguous tight pack); kernel decodes `int3(ge.bcX,...)`.
  - **#5 LOW (dead code)**: removed unused `_L` uniform from Cutting.compute + CutDetector.
  Files: Cutting.compute, CuttingCommon.hlsl, CutDetector.cs, ReconBuffers.cs (comments), CutBitPropagation test. No Stage-1/2A regression (R2 confirmed all existing strides/fields untouched). NEXT: user verifies in Unity (run GPU tests), then 2C (cut surface generation — the visible milestone).
- 2026-06-29: **CUTTING-ALGORITHM PAPER-ALIGNMENT AUDIT (vs the authoritative PDF) + dead `_CutFPExternal` REMOVED**. Extracted the real paper PDF to `paper_pdf_text.txt` (14 pp; cutting = pp.3-6, Table 3 p13) via pymupdf, then a 5-module + synthesis multi-subagent audit (workflow w4u48f528) checked the cutting code MATH + ARCHITECTURE equation-by-equation. RESULT: math overwhelmingly aligned. **Fully aligned:** outer-surface QEF (Eq2 x0=mean, Eq3 F=Σ nᵢnᵢᵀ(pᵢ−x) — the load-bearing nᵢ²=OUTER-PRODUCT question resolves CORRECT, Eq4 a=0.1(1−i/m), Eq5 box-SDF), static cut (4096 LUT, per-component cut-point CENTROID, "counted-twice" weighting, per-particle 6-edge/4-voxel stitch, shared-vertex External/Internal per Table 3 = the rim-hole fix), M-T detection (Eq6-8 byte-exact, Cramer's rule), physics (Kelvin-Voigt, 6 structural + 15 bending, diagonal shear OMITTED per paper, bending Eq14 exact, Dormand-Prince RK45 tableau exact). **Sound DECLARED deviations (code MORE correct than the literal paper — do NOT "align"):** D2 rest-length spring (paper Eq11 literal zero-rest-length would collapse the lattice), D3 a=(Fext+Fint)/m matches Eq17 (literal Eq9 −Fint would be anti-restoring), D1 (gradient at xₖ), D4/D6 (RK45 step controller fixes paper's inverted ratio). **GAPS:** (#1 CRITICAL) ablation §2.1.4 entirely unimplemented = the next stage (2D-3); (#2 important) dead `_CutFPExternal` — REMOVED THIS COMMIT (paper has no per-component External QEF; Table 3 = location labels for the ONE shared centroid; verified inert post-rim-fix; reviewed SHIP wez0uwxws — compile-safe, byte-identical render, +tidied the orphaned _VoxelCorner cut-shader decl/bind, +re-labeled ±D/2 to paper §3.4); (#3 important) `InterpCutFP` per-frame chained-EMA vs paper one-time interp — neutralized via cutFPInterp=1.0; (#4 important, USER WANTS ALIGNED) M-T detection on REST lattice vs paper's DEFORMED mesh (the rest-pose race-fix) — TODO deformed-mesh path; (#5 important, USER WANTS ALIGNED) R=polar(F) vs cited ref [22] Berndt PBD (IEEE CG&A 2017, DOI 10.1109/MCG.2017.45) — paper gives NO equation (delegates to [22]); BLOCKED on sourcing [22]. NEXT: #4 deformed detection → push aligned blade-cutting baseline → ablation 2D-3; #5 pending [22].
- 2026-06-29: **CUT RIM-HOLE (red wall ↔ green skin GAP) = the REAL delamination — FIXED by sharing one per-component _CutFP vertex (strict paper §2.1.1)**. After the EMA fix did NOT resolve it, the user ran the new debugTagColor render and isolated the bug PRECISELY: the RED cut wall (_CutFP) and GREEN cut-skin rim (_CutFPExternal) were SEPARATED by a transparent gap — a HOLE between two surfaces, not a fall artifact (demoSpinAfterCut=false → clean straight fall). ROOT CAUSE (workflow w18dv69wq, 3 readers unanimous + high conf, confirmed against paper abstract clean:33 "retains the non-manifold surface features at the cut areas"): my cut-rim work split each cut-voxel component into TWO UNSHARED vertices — green `_CutFPExternal` (per-component isosurface QEF, outer skin, bit30, DC_Stitch over surface edges) + red `_CutFP` (per-component cut-point centroid, blade plane, wall, bit31, BuildCutTriangles over cut edges). Two surfaces, two edge sets, two positions, NO shared vertex → the transparent hole. THE PAPER (clean:342-354 + Table 3) has ONE feature point per (voxel,component) = the cut-point CENTROID, referenced by EVERY triangle (skin OR wall) that touches it → structurally watertight (no weld/bridge); "External"/"Internal" are LOCATION LABELS, not two computations. The `_CutFPExternal` QEF is a paper-silent invention (the divergence). FIX (the actual paper-faithful one): `StitchVertId` (Recon.compute:240) returns bit31 → `_CutFP` (not bit30 → `_CutFPExternal`) for cut voxels, so the outer skin and the cut wall SHARE `_CutFP[slot]` → one vertex per component → watertight, no hole. + grid bump 24³/L=0.25 → 48³/L=0.125 (user choice "strict paper + raise resolution") so the paper-faithful blade-plane cross-section RIM (a voxel-resolution polygon) renders as a smooth circle — NO algorithm deviation. Trade-off (accepted): the margin is the paper's cross-section (not the round-isosurface my cut-rim work chased); the round look comes from finer voxels, not a deviation. Workflows: delam+audit (wbgysi1ul, crashed-but-recovered: physics sim falsified collapse, lower dome renders, sliver=lower cut wall) → connection-paper (w18dv69wq, the fix) → review-watertight-fix (wnkekvau9: closesHole=yes/watertight verified/no runtime regression/paper-aligned; FIX-FIRST only for a TEST that asserted the old bit30 skin → updated CutSurface Test 5 to bit31/_CutFP). `_CutFPExternal` left DEAD-BUT-INERT (never emitted → never read); full removal + the broader paper-alignment audit (±D/2 cut points, per-frame EMA, detection frame, rotation/Berndt group-4) = planned follow-up. The 3 PRIOR delamination "fixes" (box-center compCenter [no-op, reverted], cutFPInterp EMA) were WRONG — all chased the feature-point path; the real bug was the two-surface split, isolated only by the debug viz. NEXT: user Unity-verify (debugTagColor → cut surface is one RED watertight surface, no red-green gap; smooth round rim at 48³) → then full _CutFPExternal removal + paper-alignment audit.
- 2026-06-29: **CUT DELAMINATION (分层) ROOT-CAUSED + FIXED — differential temporal lag (cut wall EMA), NOT the External FP** (SUPERSEDED — the EMA was a contributor at most; the real bug was the rim-hole above). User report: after the cut, the render delaminates = rounded upper dome + dark gap + thin flat sliver below (persisted after the cut-rim work). Found via FIVE multi-agent workflows; the first three chased an External-FP hypothesis (DC_Stitch bridge / revert+gate / per-component box center "compCenter") — ALL WRONG. A faithful CPU-MIRROR investigation (2 independent pure-Python mirrors of the real 24³ demo, HIGH conf) **refuted** them: the cut-band outer skin tracks the falling piece at FULL rate (0 lag, 0 bridge quads, bimodal displacement histogram), and `compCenter` is a **no-op** (every planar-cut component is a rigid same-rest-y block per ConnectivityLUT vertToComp=[0,0,1,1,0,0,1,1]; max effect 0.0086 vs the ~0.5-1.0u a gap needs → REVERTED). TRUE ROOT CAUSE: the outer SKIN (`_CutFPExternal`→DC_Stitch) has NO temporal smoothing → tracks instantly, but the cut WALL (`_CutFP`→BuildCutTriangles) is EMA-smoothed by `InterpCutFP` at α=`cutFPInterp`=0.5 (2D-2b) → under the sustained post-cut fall+spin the wall TRAILS the skin by ≈v·(1-α)/α → dome on top, lagged flat wall sliver below, gap between = the 分层. The code ALREADY warned about this (Cutting.compute:755-757; ReconGridManager:97-98 "keep α~0.5-1.0") but shipped at the band bottom. FIX (one line): `cutFPInterp` 0.5→**1.0** → lerp(prev,curr,1)=curr → wall tracks the fall exactly like the skin → dome+sliver weld, gap closes (decisive Unity A/B: 1.0 snaps the sliver up, 0.5 reopens the gap). 2D-2b machinery preserved (α<1 re-enables); proper opening-smoothing keeper = fall-aware α (deferred). SEAM (separate user req): `bladeD` 0.04→0.02 (D/L=0.08, paper sub-voxel band) for a hairline slit. Workflows: debug wtimjivyt / paper-fork audit w6qlywnq6 / decide-cut-fix wsyuf4afr / code-review w8o1keacn / CPU-mirror wk4ggcau0. The code review of the (wrong) compCenter fix passed correctness/paper/regression, but its test-validity reviewer's rigorous CPU mirror caught it as a no-op → triggered the decisive mirror investigation. NEW test: CutFPInterp Test 6 (EMA steady-state lag = d·(1-α)/α; 0 at α=1.0) pins the mechanism. Removed broken CutRimExternalFP Test 4 (proven non-discriminating). NEXT: user Unity-verify (α=1.0 closes the gap) → then 2D-3 ablation.
- 2026-06-29: **CUT-RIM "square cross-section" FIXED — restored the paper's External/Internal feature-point split (Table 3)**. User report: the cut margin rendered faceted/SQUARE instead of following the model's original contour (must preserve the ORIGINAL form — round sphere → round, later an irregular LIVER → liver-shaped; NOT a circle-fit). Full brainstorming→writing-plans flow. ROOT CAUSE (diagnosis workflow w0kkfnvm7, high conf): the paper carries TWO feature points per cut boundary voxel — EXTERNAL (outer surface = the DC/Hermite isosurface QEF, Eqs 1-5 clean:232-296, conforms to the isosurface) and INTERNAL (cut surface = cut-point centroid, clean:343-344, on the blade plane); clean:320-321 "surface generation during cutting DIFFERS from the outer surface". The impl COLLAPSED both into one `_CutFP` and pointed the OUTER skin at it (2C-fix StitchVertId/FetchPos); the later pure-centroid holes fix put `_CutFP` on the flat blade plane → the outer skin at the cut got dragged off the isosurface = square rim. FIX (spec+plan approved): add a parallel `_CutFPExternal` = per-component isosurface QEF (mirror DC_FeaturePoints exactly: D1 projected gradient Fe=Σn·dot(n,p−x), Eq4 decay, D5 BoxSdf, seed isectMean, NO `_CutPull`; nIncl<2→isectMean; !hasIsect→w=0) → the OUTER SKIN; keep `_CutFP` centroid → the cut WALL (unchanged). NEW tag bit 0x40000000 (skin) vs 0x80000000 (wall) vs no-bit (per-voxel external); decoded at ALL 4 sites (StitchVertId/FetchPos/**ReconSurface.shader render path**/DC_NormalsScatter, mask 0x3FFFFFFF); skin normal reuses the per-voxel external normal via voxel=slot>>3 (no new normal buffers). It follows the model's ACTUAL isosurface (liver-shaped for a liver), NOT a circle-fit — per-component restriction declared paper-silent. Workflow: brainstorm → spec → plan → 3-agent plan audit (wnq0vyv8f REWORK: caught the missing render path + normals + 3 tag-decode sites → folded into plan §8) → impl (subagent, 7 files, 4 commits) → 3-agent code review (w6vi52idc SHIP-WITH-NITS, roundRim=yes, noRegression=yes, preservesOriginalContour=yes, mustFix=[]; whole-tree grep confirms NO remaining 0x7FFFFFFF → no OOB). Nits fixed: DualContouring.Build now sets `_VoxelL` (removes the CutDetector cross-object coupling); stale Stitch comment. NEW oracle CutRimExternalFP_GpuOracle (External on isosurface NOT blade / Internal on blade / distinct / per-component split / interior→w=0); CutSurface+Stitch oracles updated for bit30. `_CutFP`/wall/non-cut byte-identical (no regression to holes fix / 2D-1 / 2D-2 / 2D-2b). NEXT: user Unity-verify the cut rim follows the contour (round sphere; liver later) → then 2D-3 ablation → then cutting-completeness audit.
- 2026-06-28: **2D-2b temporal feature-point interpolation (Discussion, clean:1271-1276) — implemented, review running**. Paper (one sentence): feature-point DRIFT → sudden separation under large deformation → "interpolate the positions of post-cut feature points from the previous frame to the current frame" → visually smooth ("does not fully adhere to physical principles"). Plan → 3-agent audit (ws89m9fpw APPROVE-WITH-REVISIONS, paperFaithful=yes-with-declared-deviations) → impl (subagent, 5 files + oracle). Mechanism: per-frame EMA of the per-component cut FP `_CutFP[slot]`: new persistent `_PrevCutFP` buffer (NOT cleared by ClearCutFP, explicit zero-init); new `InterpCutFP` kernel after ComputeComponentFP, before the 4 consumers (DC_Stitch/BuildCutTriangles/DC_NormalsScatter/DC_NormalizeCutFP); `curr.w<0.5→invalid; prev.w<0.5→curr (newly-valid appear); else lerp(prev,curr,α)`; writes both _CutFP (consumers) + _PrevCutFP (next frame). `_CutFPInterp` α uniform via a new 5th Build param (default 1.0 = byte-identical off switch, back-compat for 4-arg callers); manager `cutFPInterp=0.5`. Audit-declared paper-silent: EMA is one realization of "prev→current" (steady-state lag the paper accepts); seeding "appear-at-current" targets DRIFT-SNAP not the appearance-pop (semantic: drift = post-appearance movement); 3 artifacts declared (differential-lag transient shear; EMA lags 2D-2 rotation → recommend α not too small / rotation-aware residual smoothing is the principled future fix; cumulative-recut slot-renumber smear). NEW oracle CutFPInterp_GpuOracle (α=1 no-op / α=0.25 lerp+persist / prev-invalid→appear / curr-invalid→drop / EMA convergence). NOTE: 2D-2b's benefit = smoother cut OPENING during the sweep (α<1 vs jumpier α=1); on the rigid post-cut spin it LAGS the rotation (keep α≥0.5 there). NEXT: read review wbwym9jfb → fix must-fix → commit+push → user verify (oracle green + smoother opening at α<1) → 2D-3 ablation (last cutting module).
- 2026-06-28: **2D-2 Berndt per-particle rotated frame (§2.1.2) — implemented, review running**. After the rest-pose cut shipped + pushed (43ad2ac) and the user chose 2D-2 next: plan (`docs/.../2026-06-28-dc-2d2-berndt-rframe.md`) → 3-agent audit (wsuqvg6w3 APPROVE-WITH-REVISIONS; caught 2 criticals: polar can converge to a det=−1 REFLECTION → guard det(F)>0; and ComputeParticleRot must run POST-SeverLinks else the cut-frame R is contaminated by the about-to-be-severed edge → persist R across frames, M1 ordering) → impl (delegated subagent, 10 files + oracle test). Mechanism: per-corner R = polar(F), F = Σ(d_s⊗r_s)·diag(1/Σr²) over INTACT incident edges (NbrIdx≥0); cut point stored in owner MATERIAL frame `localOffset = mul(transpose(R), P_side−pPos)` (EmitCutPoint), reconstructed `world = cornerPos + mul(R, localOffset)` (AccumulateCutFP) — the two lines AccumulateCutFP:467 always flagged for 2D. Guards: det(F)≤detEps→I (reflection+degenerate, M2), per-axis min(mx,my,mz)<axisEps→I (zero-diagonal NaN, M3), ‖F−I‖<deadband→I (keep translating tissue at R≡I so the verified demo + wggai2v70 hole fix unperturbed, M4). New RotGpu buffer (36B = 3 rows, Conn4096 stride precedent), identity-init, ComputeParticleRot kernel in Physics.compute dispatched between cutDetector.Dispatch and dc.Build, bound in CutDetector+DualContouring. 3 paper deviations declared (R=polar(F) inferred from Berndt [22]; severed-edge exclusion; identity fallback→§3.9 regime). Detection-under-rotation DEFERRED (§E; over-claiming comment reconciled). Regression: identity ParticleRot bound in CutSurface/CutBitPropagation oracles → byte-identical. NEW oracle ParticleRot_GpuOracle_Tests (30° recovers Rθ, shear→orthonormal, det<0→I, colinear→I, deadband→I, near-I regression). ⚠️ Subagent flagged a HIGH-priority uncertainty: HLSL row-major float3x3(r0,r1,r2) vs Unity.Mathematics column-major — review w4uiahvkg verifying the rotation convention is correct end-to-end (cut point must rotate the SAME way as the tissue, not inverse). NOTE: 2D-2's effect is visible ONLY under rotation (current translation free-fall has R≈I → no visual change); a rotation demo (oblique cut / pinned-edge swing) is needed to see it. NEXT: read review → fix any must-fix → user verify (GPU oracle green + a rotation scenario) → 2D-2b temporal interp.
- 2026-06-28: **Detection-band REJECTED → REST-POSE DETECTION (the durable, paper-grounded hinge fix)**. Review w4amk1gw7 (3 reviewers, adversarial = CHANGES-NEEDED) killed the detection band: bladeY=3.125 sits EXACTLY L/2 from BOTH corner layers, so a symmetric t-band over-cuts the upper layer once the tissue pre-sags ~0.06-0.08L (under-damped ζ≈0.0017) → severs the y=3.25 layer from both neighbours → free-floating 1-voxel disc + ~2L kerf, PERMANENT (mask never resets). The adversarial reviewer's better fix (user chose it via AskUserQuestion): **detect WHICH edges are cut against the UNDEFORMED (rest) edge** (O_rest=_GridOrigin+coord*_VoxelL, the uniform lattice BackgroundGrid.cs:107) → cuts a fixed MATERIAL plane, immune to the gravity-vs-sweep race → 257/257, ZERO over-cut, deterministic; **emit the cut POINT on the DEFORMED edge** (P_hit=O_def+t_ray*Dvec_def) so the surface tracks the tissue. Plan→3-agent audit (w87ghv7f6, APPROVE-WITH-REVISIONS, closesHingesNoOvercut=yes)→impl (this commit). DECLARED DEVIATION (audit R1): the paper M-T-tests the DEFORMED mesh §2.1.3 ("no longer a uniform hexahedron" clean:357-358); the "no deformation" clause (clean:363-366) governs the cut-point RECORDING frame, not the test — detect-on-rest EQUALS the paper for pure translation (demo X/Z motion≡0 by load symmetry about x=3,z=3, pure −Y force) and is the foundation for 2D-2 (transform the world blade into each particle's material frame via the [22] rotated axes). Files: Cutting.compute (DetectCut: O_rest/Dvec_rest NEW locals for cull+M-T, deformed O/Dvec kept for P_hit; +`_VoxelL`/`_GridOrigin` uniforms), CuttingCommon.hlsl (MollerTrumbore band reverted → exact Eq6-8), CutDetector.cs (Dispatch +L+origin params, binds, un-inflated AABB, band fields removed), ReconGridManager.cs (pass L+Vector3.zero, +z-step<L coverage guard), CutBitPropagation/CutSurface tests (bind _VoxelL/_GridOrigin = build L/0 → rest==cornerPos → assertions unchanged). All 6 must-revise (R1-R6) folded in. Band fully reverted (grep clean). Review wr3ajdsnm running. NEXT: read review → user Unity-verify cutPoints→~514, severedNbr→~4264, lower half FALLS MONOTONICALLY (no hinge, no floating disc, no 2L kerf) → then 2D-2 + 2D-2b.
- 2026-06-28: **INCOMPLETE-CUT (residual hinges) root-caused (9-agent workflow wpm9fwlmt, 3/3 skeptics refuted=false) + DETECTION-BAND fix (later rejected, see above)**. The lower hemisphere stays attached ("一段切了一段没切"); console: cutPoints freezes at 466 = **233 edges**, severedNbr +466 (1:1 sever-on-detect), lowerMinY OSCILLATES (-0.3..-2.7, non-monotone) = lower mass HELD by hinges, NOT free-falling (corrects the prior wrong "free-falling" claim). **ROOT CAUSE = detection-vs-gravity race:** the swept cutting quad is a ZERO-Y-thickness plane FIXED at world Y=bladeY=3.125, but DetectCut M-T-tests it against DEFORMED corner positions; the ~1s progressive sweep lets gravity sag a Y-edge >L/2 out of that thin plane before its Z-band is tested → t_ray leaves [0,1] → no hit → cut bit never set → SeverLinks (gated EdgeIsCut) skips it → permanent hinge. Rest-pose enum: **257** occupied Y-edges, blade hits **257/257** in rest pose; runtime 233 → **24 hinges**. Blade span/range/thickness (D used ONLY in EmitCutPoint offset, NOT detection) are red herrings; severing topology is COMPLETE (Agent D: a clean planar cut fully disconnects, 0 independent bridges). **DECISIVE: NOT a 2D-2/2D-2b problem** — paper (clean:362-368, 1269-1276) says both are cut-SURFACE smoothing, ORTHOGONAL to link severing; Fig 3.9 (clean:1157-1164, one point fixed + gravity + cut-while-falling) claims FULL disconnection with this exact protocol → completeness is achievable, this is a separate detection-coverage gap → **fix FIRST, then 2D-2/2D-2b** (per user). **FIX (algorithm-level, not a demo timing hack):** a DETECTION BAND — widen ONLY M-T's finite-edge t-clamp by tBand·|edge| (CuttingCommon.hlsl MollerTrumbore gains `float tBand`; t∈[−EPS−tBand,1+EPS+tBand]; u,v stay tight = exact blade X-Z footprint), so a Y-edge sagged up to ~tBand·L out of the plane still registers. Cutting.compute: new `_CutEdgeBand` uniform (=0.45) to both DetectCut M-T calls; P_hit clamps t_ray to [0,1] (cut point stays on the real edge); MT_Test passes 0.0 (exact Eq6-8, oracle unchanged). CutDetector.cs: CutEdgeBand=0.45 + AabbInflate=0.30 fields, binds the band, INFLATES the swept AABB (degenerate in Y → would else cull the sagged edge before M-T). ReconGridManager sets AabbInflate=L. Band math: 0.45 catches sag up to 0.95L (t=0.5+4s≤1.45→s≤0.2375) while EXCLUDING rest-pose adjacent layers (at t=±0.5). DECLARED DEVIATION (Eq6-8 math preserved, only finite-edge acceptance widened; spirit = paper's persistent swept plane "prevents collision detection failure due to rapid movement", clean:374-382). Backward-compatible (band defaults 0 → exact original). Review workflow w4amk1gw7 running. **Residual risk to watch:** if a column sags as a BLOCK >0.95L the band can't catch it (fallback = larger band or cut-then-release diagnostic). NEXT: read review w4amk1gw7 → user Unity-verify cutPoints→~514 + lower half FALLS monotonically → then 2D-2 + 2D-2b.
- 2026-06-28: **"洞/holes" diagnosed (3-agent workflow wggai2v70) + cut-FP → PURE CENTROID (paper §2.1.1)**. Definitive diagnosis of the user's "切割不连续，只有一些洞" + "没掉落/震荡": (a) the cut + SeverLinks ARE correct and the freed lower half IS free-falling — the [FALL] readback's `lowerMinY` oscillation was an INSTRUMENT artifact (it re-scans every corner currently below the plane, mixing the swinging PINNED upper hemisphere into the "lower" set). (b) The HOLES = cut feature points jittering frame-to-frame: `ComputeComponentFP` ran a PER-FRAME QEF gradient descent (isect-plane D1 + `_CutPull` + D5 box-stop) over the DEFORMED isect inputs → the dual vertex moved every frame → `_CutFP.w` flipped valid/invalid → `BuildCutTriangles` (skips `w<0.5`) dropped quads. (c) The big OSCILLATION = under-damped relative-only Kelvin-Voigt + only the thin top cap pinned (upper hemisphere swings as a pendulum). **FIX #1 (this commit, paper-strict):** replaced the QEF re-solve with the paper's PURE CENTROID of the component's cut points (`_CutFP[slot]=float4(cutC,1)`; newpaper_clean.txt:343-344) — deterministic per cut-point set, no jitter, and (the two components' centroids already ±D/2 apart) the per-component split + gap==D are preserved. Removed dead `_QefIters`/`_L`/`_CutPull` from the cut shader (stay live on the Recon DC_FeaturePoints path). 1-agent focused review: APPROVED-WITH-NITS (paper-exact §2.1.1; split intact via per-component slot keying in StitchVertId/BuildCutTriangles; deterministic; tracks the falling half's TRANSLATION watertight — boundary lip now a bounded sub-voxel notch, accepted). NOTE: still STATIC world cut-point offset → only TRANSLATION tracked; ROTATION drift (swinging upper) still needs **2D-2 Berndt R-frame** + **2D-2b temporal interpolation** (next). NEXT: user Unity-verify the FALLING piece's cut surface now has FEWER holes / is cleaner; then implement 2D-2 + 2D-2b.
- 2026-06-28: **2D-1b TISSUE-ONLY PHYSICS + horizontal cut (the real "make it fall" fix)** — 2 debug workflows (w8tio0dk5, w40o668qp) + impl + 2-reviewer review. ROOT CAUSE of "no piece falls": severing WAS correct, but the physics ran on the FULL 25³ lattice (incl. empty corners) while the cut is occupancy-gated to tissue, so the never-severed empty-space spring/bend "CAGE" hung both halves from a pinned corner layer that sits in EMPTY space above the sphere (pin y=6 vs sphere top y=5). Violated paper §2.1.4 (outside-isosurface particles are deleted/inert). FIX (paper §2.1.4): per-corner `cornerActive` (= corner of any occupied voxel = inside + one-ring shell); BuildPhysics skips springs/bend-pairs touching an inactive corner; Physics.compute freezes inactive corners like pinned (ComputeSlope/IntegrateY5/ComputeError) + `AccumulateStructural` skips `_Active[n]==0` (load-bearing — force path is NbrIdx-gather, not springs[]); new CornerActive buffer. Demo: pin the top TISSUE cap (active && y≥top−L), re-orient to a HORIZONTAL cut (Fig 3.9: blade = X-line at bladeY=center.y+L/2, swept Z, n_cut≈−Y, severs Y-edges) → lower hemisphere free-falls (translation, no rotation → also dodges the deferred 2D-2 R-frame → clean walls). Also fixed EdgeIsCut to OR across stencil voxels (boundary perimeter ring). Reviews: R1 physics PROVEN (cage fully removed, piece WILL fall, §2.1.4-faithful), R2 GPU READY; fixed 2 test regressions (StructuralForce + Rk45 oracles now bind/replace _Active). NOTE: this is also the substrate for 2D-3 ablation (delete == active=0). Bounce = under-damped Kelvin-Voigt (paper model); flicker = deferred 2D-2/2D-2b. NEXT: user Unity verify the lower hemisphere FALLS (set Inspector bladeD=0.04, bladeSweepRange=2.2, bladeSpan=1.4, pinTopLayer=✔).
- 2026-06-28: **Seam narrowed + 2D-1 PHYSICAL SEVERING done** (full workflow: plan → 3-subagent audit wabip73b7 → impl → 2-reviewer code review, all APPROVED). Seam: bladeD 0.12→0.04 (sub-voxel §3.4) + _CutPull 1.0→4.0 (gap==D, no jaggedness; commit 137e13f) — user confirmed visibly thinner+smooth. **2D-1 severing (the BLOCKER → cut becomes REAL):** new `SeverLinks` kernel (Cutting.compute) — per cut grid edge, writes _NbrIdx=-1 BOTH directions (sPlus=2axis+1/sMinus=2axis vs NbrDelta) + zeros BendPair.alive for pairs whose arm crosses the edge; force kernels already honor the guards (Physics.compute:94 n<0, :128 alive==0) so SeverLinks is the SOLE writer (RW in Cutting, SRV in Physics, race-free). Wired into CutDetector.Dispatch after DetectCut (idempotent, every frame). BendPairGpu guarded-local struct (no PhysicsCommon include). Demo bladeSweepRange 1.5→2.2 (full sphere z-traverse → full separation). Sever_GpuOracle_Tests (both NbrIdx slots→-1 + bridging pairs alive==0 + no-overkill + idempotent). Reviews: R1 physics PROVEN (paper:367 necessary+sufficient; both-direction+bending-scan complete), R2 GPU integration READY, zero-cut byte-identical. **Audit also caught a MISSING paper module: temporal cut-FP interpolation (paper:1270-1276) → roadmap 2D-2b.** Roadmap + cut-FP→D7 ledger items recorded. NEXT: user Unity verify (set Inspector Blade Sweep Range=2.2 + Blade D=0.04 — serialized scene overrides code defaults!) → expect lower half FALLS under gravity after the sweep traverses. Then 2D-2 dynamic frames.
- 2026-06-28: **2C "no visible cut" diagnosed + FIXED** (3-subagent runtime audit → fix design → 2-auditor plan audit → impl → 2-reviewer code review; see findings.md). Two architectural gaps the green tests couldn't catch (AllInsideLS → empty skin): **RC1** the outer DC skin never split per component (cut walls hidden inside a closed sphere) — fixed by making `DC_Stitch` component-aware (per-component dual vertex via `ComputeComponentFP` = unified QEF over component isect planes + cut centroid; same `LocalCornerOf`/`Conn4096Comp` keying); **RC2** cut geometry in empty non-tissue voxels (the gray flap) — fixed by a per-voxel occupancy gate (`VoxelOccupied` = OR of 8 corners inside) on DetectCut/Lookup/Accumulate/ComputeComponentFP/BuildCutTriangles/DC_Stitch. Also: `IsectGpu._pad`→`int insideA` (inside-endpoint isect→component assignment, the load-bearing separation mechanism); blade offset to mid-edge (x=center+L/2). Reviews: R1 re-weld-PROOF + separation ≥D guaranteed (hand-derived 2× margin; isect normals ⟂ cut axis so gap can't collapse) + paper-faithful; R2 stride/includes/bindings/dispatch/zero-cut all PASS. Reserved: projection safeguard for OBLIQUE cuts (2D). NEW PAPER-SILENT D-item: fused per-component QEF+centroid vertex. NEXT: user verifies VISIBLE slit in Unity, then 2D.
- 2026-06-28: **2C (cut surface generation — the VISIBLE cut) COMPLETE & reviewed.** Workflow: detailed plan (REV 2 after 2-auditor audit: A1 algorithm proved re-weld-safe + found C1 edge-predicate/C2 winding-under-CullBack; A2 GPU found F1 int4-InterlockedAdd-invalid + integration gaps — all folded into plan) → subagent-driven implementation → 2-reviewer code review (R1 opus algorithm APPROVED re-weld-proof; R2 sonnet integration READY). 6 new kernels in Cutting.compute (ClearCutFP, LookupConnectivity, AccumulateCutFP[fixed-pt centroid], FinalizeCutFP, BuildCutTriangles[per-particle Fig 2.5 stitch + EmitCutQuad partial-quad + internal tag 0x80000000|slot], DC_NormalizeCutFP); 6 new buffers (CutFP, CutFPAccumPos/Cnt FLAT int atomics, VoxelFPCount, CutFPNormal/F); LocalCornerOf (GridConventions + HLSL); POS_SCALE 1<<14; tag-aware DC_NormalsScatter (FetchPos per-vertex for mixed tris) + ReconSurface.shader Cull Off + two-sided (SV_IsFrontFace); DualContouring cross-shader orchestration (2-arg ctor/SetCuttingShader, null=Stage-1 byte-identical); ReconGridManager auto blade-sweep wiring + triCapacity += 2*GridEdgeCount + mat binds. CutSurface_GpuOracle_Tests (LocalCornerOf round-trip CPU + connectivity/centroid/count-divisor/no-reweld GPU). **Re-weld safety = vertex-keyed chain proven intact (the MC2024 failure mode is structurally prevented).** NEXT: user verifies VISIBLE cut in Unity (assign Cutting.compute on manager; expect a slit opening through the deforming sphere, gap≈D, both walls lit, FPS stable), then 2D (dynamic frames + spring/bending sever + ablation — physical separation).
- 2026-07-02: **Stage 4 — §2.1.2 dynamic cutting: MATERIAL-SPACE RIBBON detection (gravity holes fix), IMPLEMENTED + 3-agent review PASS.** Root cause (3-agent diagnosis wf_e3f28151, unanimous; CPU proofs scratchpad/tunnel_repro.py + tick_validation.py): world-space detection space-time-undersamples moving tissue — LCS_Step jumps tissue once/frame, rod substeps detect with tissue frozen; the coincidence point races along edges faster than the blade so even 300× denser world sampling never forms a separating set (comps=1); zero-g exact 3-way equality explains the perfect 0-g cut. FIX (deviation D7): detection ray = UNDEFORMED rest edge (§2.1.2 "edge has not undergone any deformation"); cutting plane = §2.1.3 swept plane mapped to material coords (rod samples ~0.5L → continuous inverse-trilinear (Newton, deterministic occupied-preferred claim) → prev/cur ribbon strip); ribbon EXTENDED AFTER EVERY ACCEPTED RK45 SUBSTEP (cut_ribbon_tick from physics_step — kills oscillation aliasing, D2 blocker; CPU: TICK=8 → 0 holes vs fine reference) + per-C#-rod-substep (tissue frozen). Zero-deformation ⇒ identity map ⇒ ribbon == paper's two-triangle quad (0-g behavior unchanged). Guards: 3L world-proximity clamp (phantom-cut), sliver-area floor, 1e30 resolve-divergence demotion, NS-change prev-polyline resample, tick-error surfacing to LCS_Step rc, parked-blade perf gate. Review wf_d8a1fc51 (fidelity/CUDA-host/regression) all PASS, 12 minors → 6 fixed + D7 ledger entry; delta-verify wf_cd5d6da7. Downstream (mask/conn4096/sever/FP chain/[22] Fig4 frames) untouched. DLL build/Release ready; Unity runtime verification pending (user).
- 2026-07-02 (b): **Stage 4b+5 — residual gravity holes ROOT-CAUSED + FIXED; cut-margin staircase (棱状凸起) FIXED per paper Table 3. All review agents pass (V3's sole major = deploy step).** Diagnosis wf_19bb05de (3 agents + CPU instruments hijack_exp3.py): (1) gravity holes = unoccupied SHEAR-SHELL voxels (mixed fallen/frozen corners) claiming rod samples when occupied containment fails → material points displaced up to 8.7 voxels → 3L clamp rejects the detoured marks → mark-free holes → comps=1 (Unity signature reproduced in-model; occupied-only claims → comps=2, clampRej=0; "≥1 active corner" rule proven bit-identical/useless; zero-g clean because no folds). (2) staircase = cut voxel's single per-component FP (cut centroid) hijacking ALL skin stitches (dc_recon StitchVertId) — paper Table 3 mandates coexisting External+Internal FPs. FIXES: cut.cu k_MapRodClaim occupied-only early-out; median-offset outlier gate (2.5L); bracketed LINEAR offset interpolation (kills G2's 17-hole constant-offset floor); g_lastOffAge per-detect aging; C# RodSpanLength from DEFORMED extent + coverage warning (scene had autoSizeRod:0, rodLength 12 < 17.8!). Stage 5: d_extFP per-(voxel,component) skin QEF (Eq2-4+D1+D5, mirrors k_FeaturePoints, recon_qef_iters()) + wall centroid kept; tag 0xC0000000|slot skin / 0x80000000|slot wall; StitchVertId cascade skin→wall→voxelId; FetchPos/NormalsScatter/Expand 3-way; Fig 2.3 rim quads in k_BuildCutTriangles; Clear/Interp/Normalize both families; recon_set_cut_buffers 9-arg. Reviews wf_9c705a80: V1 PASS(high) V2 PASS(high) V3 code-clean (7/7 dims; fail only = stale DLL in Assets). Final DLL build/Release md5 e85bebf3efebfc91a3b448d99105295f — USER: close Unity, copy to Assets/Plugins/x86_64, tick autoSizeRod in EDIT mode and save the scene.
- 2026-07-02 (c): **Stage 6 — CCD detection architecture + [22] severed-edge isect rebuild + debris-trimmed rod extent. ALL reviewers pass (S1 blocker formally refuted on re-adjudication).** After 3 fixes the Stage-4 inverse-map still left ~512 interior holes at real folds → architecture replaced (Phase 4.5): rod phase = §2.1.3 verbatim world quad M-T vs DEFORMED edges (the proven 0-g path, k_DetectCutQuad); tick phase = CONTINUOUS COLLISION of each moving edge vs the static blade segment per RK45 substep (k_DetectCutCCD; blade static + linear endpoints ⇒ coplanarity QUADRATIC, solved exactly — no tunneling, no inverse map, no claims/extrapolation/3L clamp; all deleted). CPU proof ccd_validation.py: ZERO symdiff=0 (0-g byte-identical); FAST T8==T64 EXACT (247/247, 0 holes). [22]-mandated fix (user spotted it): k_RebuildIsectWorld now REBUILDS severed surface edges' missing endpoint from the inside particle's frame + undeformed length (insidePos + R_inside·(±L·axis)) instead of lerping across the gap — fixes horizontal-cut margin ridges (gaping Y-severed edges) AND fallen-fragment dragging strips; explains why vertical cuts and 0-g were clean. C#: deformed extent percentile-trimmed (falling debris had inflated it 231→250). Reviews wf_98fc8425 (S2/S3 PASS; S1's blocker was built on the stale pre-fix validation output) + re-adjudication wf_dbf90148 (blocker_upheld=false, union completeness verified seam-free; residual = frame-granularity splitting DISPLACEMENT only, documented). All 8 minors fixed (per-hit CCD emit normal, activation-time snapshot seed, coplanar-slide note, nbrIdx invariant note, RotGpu reuse, 4 stale docs, dead helper removed). FINAL DLL md5 below — USER: close Unity, copy build/Release/LiverCudaSim.dll → Assets/Plugins/x86_64, tick autoSizeRod in EDIT mode, save scene.
- 2026-07-02 (d): **Stage 7 — paper-exact SINGLE FP per (voxel,component) + air-edge gate + oriented walls; Bug-B coverage package. ALL final reviewers PASS (R1/R2/R3 high).** Diagnoses: wf_60781cea (A1 harness: cut points recorded in the AIR part of occupied surface voxels, wall FPs up to 0.72L above skin => durian bumps on skin-parallel cuts, 10x vs vertical; A2: THE PAPER HAS NO RIM BAND AND NO DUAL FP FAMILIES — one FP per component seeded by iso isects + cut points TOGETHER ("cut edges calculated twice"), Table 3 External/Internal = positional labels, skin+wall SHARE the vertex; A3: stencil-order wall winding left one full lip inverted 224/224) + wf_65901910 (B1: gravity failure = SWEEP COVERAGE, X=272 vs ~650 needed — dump numbers reconcile exactly; holes=0 was a single-plane classifier false-negative; B3 package spec) + wf_6647e0ee (full paper audit: C1/C2/C4 aligned, C3 sole mismatch = globalDamp → D8). FIXES: Stage-5 dual-FP+rim DELETED, k_ComputeComponentFP = combined seed + Fig2.2 iteration + D5 clamp (R1 verified quantitatively this does NOT reintroduce the staircase); cornerInside uploaded via new LCS_InitCut param, both detect kernels skip both-endpoints-outside edges; EmitCutQuad oriented by refDir toward the severed opposite endpoint; effRodLen=max(rodLength, anchored-component span) unconditional; anchored extent via 1Hz union-find; PATH-WIDE swept-voxel ribbon bridge classifier + live HUD; CCD skips severed edges; scene autoSizeRod=1; DeviationLedger.cs D7/D8/D9. FINAL DLL md5 ab8bd5dec0c820e1c76877d496e57b0a — USER: close Unity, copy build/Release → Assets/Plugins/x86_64 (Unity auto-recompiles the C# ABI change), test 0-g horizontal + vertical + gravity full cut, press P.
- 2026-07-02 (e): **Dashed-slit regression ROOT-CAUSED + FIXED (air-gate v2); 5-agent panel: P1/P3/P4/P5 PASS, P2's sole blocker = the manual DLL deploy step.** Root cause (P1 ran the actual ConnectivityLUT: gate mask=10 -> compCount=1, v2 mask=170 -> compCount=2; explicit air bridge V0-e0-V1-e9-V5-e5-V6-e10-V2): Stage-7's air-edge gate skipped MASK marking for both-outside edges, so plane-crossed air edges left the outside corners connected and conn4096 fused the two sides into one component -> skin never split -> dashes (0-g Y severed 529 perfect-era -> 287). Paper Table 3's "cutting edge = an edge that intersects the cutting plane" has NO inside qualifier for exactly this reason. FIX v2: MarkAndEmit marks UNCONDITIONALLY (paper-literal), emitOk gates only cut-point emission for both-outside edges (the actual A1 durian source — P4 traced: emission set byte-identical to v1, durian cannot reintroduce; starved air comps get w=0 FPs -> wall quads skip them). P3 literature verdict: "our architecture matches — and on tissue-motion CCD exceeds — the state of practice" (Dick/Georgii/Westermann 2011 disconnects ALL intersected links, deformed-state swept-front test = our world-quad M-T; Splitting Cubes = conn4096 ancestor; optional upgrades noted: Hermite cut-FP placement, dual cut distances per link). P5 adjudicator: SHIP IT (own topology sim airedge_gate_check.py: v1 comps=1/severed 54, v2 comps=2/severed 63). Ribbon classifier narrowed to straddle-AND-in-ribbon. Queued minors: SweptKey negative-cell guard; all-unoccupied-stencil unseverable-edge fallback (verify occurrence first). FINAL DLL md5 f5669f594e1e1e122cddeacfd6a5877a.
- 2026-07-02 (f): **Fix v3.1 — pocked-slit + gravity striations. Full user-mandated process: 4-agent diagnosis (wf_2b56575e unanimous) -> plan v3 -> plan review (wf_038eabf9: Q1 REJECTED with 2 exact corrections, Q2 approved) -> v3.1 implemented per Q1's own corrections -> final review (wf_40d34080): F1/F2 PASS high, sole major = manual DLL deploy step.** Diagnosis: detection HEALTHY (gravity dump comps=2, 2596-corner piece falling, holes 0/0; 0-g rectangle groove correctly comps=1); residual visuals = (i) v2 emission gate starved pure-air rim comps (D1 3 proven invariants; D3: paper emits for ALL cutting edges; D4 harness: only wobbly real strokes reproduce it — variant c strictly best) and (ii) NEW D2 blocker: all-8-corner D5 box center collapses straddling voxels' FPs into the void after separation => striations. v3.1: emitOk fully removed (paper-literal); per-COMPONENT D5 box (mean of comp's own corners — reduces to all-8 pre-cut); pure-air = comp contains NO phi<0 corner (Q1a); D4c mean-plane projection AFTER the clamp (Q1b, verbatim formula, 1e-6 guard); k_AccumulateCutFP OOB guard + C# capacity 4x. Queued cosmetics: dead cornerInside params in detect kernels, 4 stale comments. Known rare residuals (documented): compCount==1 pinch voxels, <3-occupied rim edges, nLen<=1e-6 thin-slab projection skip. DLL md5 7457869fc941cbe6115bde1fda1fb22d — USER: close Unity, copy build/Release -> Assets/Plugins/x86_64, verify md5, reopen (C# 4x capacity recompiles), test wobbly hand strokes (straight scripted planes cannot reproduce the old holes) in 0-g AND gravity, press P.
- 2026-07-02 (g): **Fix v4.1 — strands/fans + wide kerf + debris specks. Full mandated process: 3-agent diagnosis (wf_f1f3b1c5 S1/S2/S3) -> plan v4 -> plan review (wf_b08c3fc2: R1 5 amendments, R2 GO) -> v4.1 implemented with ALL amendments -> final review (wf_1c1aaa28): V1/V2/V3 ALL PASS high, 0 blocker/major; 3 reviewer-suggested minors applied (P1b comment scope, dangleRefs->max slot, LCS_Finalize rc check).** Diagnoses: (A) strands/fans = EMA slot-aliasing (slot 8v+comp renumbered by conn4096 on mask growth -> lerp across LIPS) + EMA free-fall lag (cut FPs smoothed, skin FPs not) + single-particle runaways (all-6-severed corners integrate forever); (B) kerf ~0.44L not D=0.16L: iso isects dominate the FP seed and the Fig-2.2 refinement had NO cut-plane term (cut points carried no normal) -> FPs settle ~0.22L/side, x 1/sin(theta) on grazing horizontal cuts; (C) 0-g residual split into pinch-voxel vs rim-hole classes, measurable. FIXES: P1a k_InterpCutFP mask-epoch invalidation (d_prevVoxelMask rolls at end of chain; mask growth == the ONLY re-key trigger, proven complete) + 0.5L jump gate (rigid motion snaps; alpha-independent); P1b k_DeactivateSevered (all-6-severed -> active=0, no false positives at lattice bounds) + inactive-owner skip in k_AccumulateCutFP (interior specks vanish, w=0; skin-touching crumbs keep a frozen iso-seeded patch — accepted limitation); P2 CutPointGpu pads -> matNrm=R_emit^T*(side*ncut) (32B intact), per-slot world-normal accumulator, k_ComputeComponentFP tail = refinement -> D5 clamp -> D4c -> WALL PROJECTION LAST (no re-clamp; degenerate guard |nAcc|>max(1e-6,0.25*ccnt)) -> kerf = D exactly, no 1/sin(theta); P3 [DEBUG-CUT]: d_dbg[16] (ema pairs/jump>L/jump>3L/maxDisp/new/epoch; dangle/giantWall/Mixed/Skin/maxEdge via k_DebugScanTris in dc_recon (cross-TU hook recon_debug_scan_tris); rim<3 basedHere-gated; pinch1; all6severed/frozen) + LCS_GetCutDebug(out18,alpha) + C# PollCutDebug EVERY frame, host aggregation, 1Hz + on-anomaly print with RUNTIME alpha; P4 cut.h capacity comment. V2 quantified: S2's 0.22L seed error fully removed; residuals documented (mixed-side pinch slots bounded <=D/4; wall-pull can exit D5 box bounded ~2L, watched by dbg[7]). DLL BUILT + DEPLOYED to Assets/Plugins/x86_64 (Unity was closed), md5 25f7751efb8c6c868adce085a2f27a17. USER: reopen Unity (C# recompiles: new extern + per-frame debug poll), run gravity + 0-g cuts, send the [DEBUG-CUT] / [DEBUG-CUT][ANOMALY] lines — each counter adjudicates one hypothesis (jump>L|epoch=aliasing was live; maxDisp~1L&jump=0=lag; dangle/giantWall=fan source; rim<3/pinch1=0-g residual class; all6/frozen=debris; raw/cap=starvation).
- 2026-07-02 (h): **Fix v5.1 — gravity strand/fan (藕断丝连) ROOT-CAUSED via the [DEBUG-CUT] run + FIXED with the D10 tear law. Plan review wf_c416ae77 (R1 approve / R2 2 majors adopted), final review wf_e18cbc60 W1/W2 BOTH PASS high, 0 blocker/major.** User's instrumented run was decisive: jump>L=0/maxDisp<=0.32L/dangle=0/debris~0/raw<<cap EXONERATED EMA-aliasing, EMA-lag, dangling refs, debris, starvation in one run; the sole anomaly = PURE WALL giants (giantWall 0->45, maxEdge 2.15L->51L growing with fall AFTER cutPts froze at 2088; giantMixed=giantSkin=0; pinch1~110 at the rim; 0-g clean). MECHANISM (all 9 observations consistent): UNSEVERED RIM BRIDGE edges (sweep-boundary edges the ribbon never crossed) + NO fracture law — the falling piece (~2500 corners, restrained negligibly by 2-3 ks=10000 springs) falls at terminal v=g/globalDamp=2u/s (log range-min slope 1.9u/s EXACT) while bridge voxel chains stretch 10-50L; those voxels' comps LEGITIMATELY contain dragged+staying corners (the bridge is really uncut, conn4096 correct), so per-comp D5 box + cut centroid put a VALID FP mid-gap and its wall quads stretch with the fall. CCD cannot re-cut (fall path misses the parked blade). FIXES: (P5a/D10) k_TearOverstretch per ribbon tick between k_DetectCutCCD and k_SeverLinks: intact active-active edge with len > tau*L (tau=tearStretchRatio inspector field, DEFAULT 4.0 per R2-m1 — must clear legit sag strain; bridges blow through any tau in 1-2 frames; <1/NaN sanitized to OFF in cut_init) -> full MarkAndEmit (tear plane ⊥ edge, midpoint hit, side signs verified A=-1/B=+1 opening a D gap) -> conn4096 splits, FPs recover to lips, torn face renders a real wall, same-tick SeverLinks severs spring+bends; EdgeIsCut early-out (same-tick CCD overlap, W1-m2) + all-unoccupied-stencil early-out (R1-m3 refire) + already-severed/active gates. (P5b) EmitCutQuad pairwise (6L)^2 gate over the accepted FPs BEFORE triCounter add (R2-m2: 6L clears legit sag ~3.5L; runaways reach 10-50L) — gated quads never enter d_tri so giantWall stays a true meter. (P5c) dbg[15]=tears CUMULATIVE (chain memset now slots 0-14 ONLY — R2-m3 caught the same-frame zeroing that would have read 0 forever); C# prints tears=, slot 15 max-aggregated. ABI: CutInitDesc += float tearStretchRatio (END, both sides rebuilt together; existing scenes take the 4.0 class initializer). EXPECTED POST-FIX: tears>0 at separation, giantWall->0, maxEdge bounded ~4-6L transient, clean detach, 0-g byte-identical (tears gated on toolSet + tau>>static sag ~3%). FINAL DLL md5 b8eeb9cb5c55aa882772621dd69801ff — USER: close Unity, copy build/Release/LiverCudaSim.dll -> Assets/Plugins/x86_64, reopen (C# recompiles tearStretchRatio field), re-run gravity cut, send [DEBUG-CUT] lines (watch tears=/giantWall/maxEdge).
- 2026-07-02 (i): **Liver surface rendering IMPLEMENTED — PhotorealisticLiver (URP reference project) ported to the Built-in DC liver; design doc APPROVED over 3 review rounds (wf_ce80c1fe/wf_9078a66a/wf_43e82691), implementation passed stage-1 (wf_c1b7d14b native), stage-2 (wf_07417a1f Unity), and the FINAL 3-agent review (wf_cc5af55e F1/F2/F3 ALL PASS high, 0 blocker/major beyond the known DLL-deploy user step).** Feature: rest-position triplanar albedo/normal/spec (Ben Golus Whiteout) + Liver_GGX Cook-Torrance + wrapped-diffuse/backlight SSS + Schlick fresnel rim, double-sided VFACE — all math byte-verbatim from the reference shader; UV1 = float4(rest anchor, wall flag) generated in the EXISTING k_ExpandVertices pass (d_voxelExternalFPRest + d_cornerPosRest init-time snapshots; anchor = rest skin FP or rest voxel center); wall flag = bit30 ORed at the k_BuildCutTriangles tag (0xC0000000|slot — proven transparent: all idx decodes mask 0x3FFFFFFF, all cut-FP tests bit31-only); cut walls (blade/CCD AND v5.1 tears — same emitter) render matte _InteriorColor, no fresnel, hard lip transition exactly at the shared component FP => texture is "cut" with the tissue (skin never smears into the wound; fallen pieces keep anchors glued; voxel first-cut tag switch is anchor-bit-identical = no pop). C# vs CUDA verdict (A2, measured): C# suffices — aux = one float4 store/index in the existing kernel + ~5MB/frame readback (~0.5-1.6ms) on the pre-existing copy path; staged plan documented (SetVertexBufferData pass; D3D11 interop only for VR/grid-upscale). Files: LiverSurface.shader(+meta, ASCII, ForwardBase+ShadowCaster, target 3.0), Assets/ReconGridDC/Textures/liver2+bump+spec(+hand-authored metas: albedo sRGB1, bump NormalMap type=DELIBERATE deviation from reference Default import, spec sRGB0 mirrors reference), LiverCudaManager.cs (serialized shader+textures+triplanarScale=0.07, STRICT fallback shader-or-any-texture-missing -> CudaSurface, material-before-first-build, live mesh.bounds (falling-piece frustum fix), EntryPointNotFound stale-DLL guard), dc_recon.cu/.h + plugin_api.cpp (recon_get_surface_aux/LCS_GetSurfaceAux). DLL md5 8d66f608f28bd300b9e7a47ddc92af2f — USER: close Unity, copy build/Release -> Assets/Plugins/x86_64, reopen, assign on LiverCudaManager inspector: Liver Surface Shader = LiverSurface.shader + the 3 textures from Assets/ReconGridDC/Textures (until assigned it falls back to flat CudaSurface by design), verify: textured liver, texture glued under sag, cut walls interior color, fallen piece keeps texture.
- 2026-07-02 (j): **Render correction — pale-liver first run ROOT-CAUSED (wf_413cc887, 3 agents high-conf) + property/meta-only patch, review wf_ae68d357 C1/C2 BOTH PASS (color algebra verified in linear space).** Causes: (1) WRONG LOOK SOURCE — the user's expected screenshot is the reference CuttingV3 pipeline whose tissue shader is LiverTissueGPU (texture-NATIVE albedo = tex x tissueTint(1.04,0.68,0.52) x 0.82 => liver2's own deep red-brown), not PhotorealisticLiver whose tex x darkred x 3.5 law caps G<=0.157/B<=0.105 (hue crushed to red, then white F0~0.36 + skybox SH wash the clipped result to pale flesh-pink; preview ball stayed dark = shader fine, data/params wrong); (2) tiling 10-20x too coarse (0.07 x 17.8u = 1.25 tiles; liver2's 40%-of-tile dark blotch became the half-liver patch); (3) bump PNG is GRAYSCALE height but meta had convertToNormalMap:0 => UnpackNormal garbage. PATCH (no shader/native changes): triplanarScale 0.07->0.55 (~10 tiles) + serialized-scene migration (0.07 auto->0.55 at Play, logged); _Color=(0.2437,0.1593,0.1218) [= tissueTint x 0.82 / 3.5 — exact algebra through the unchanged tinted=tex*_Color*3.5 law, verified to 1.5e-4], _TextureStrength=1.0; wetness pack _Roughness 0.12 (effective 0.072 ~= reference smoothness 0.939), _SpecularStrength 1.0, _SpecularColor (0.30,0.27,0.25) [F0~0.13], _FresnelStrength 0.5, _SSS 0.12/0.15; bump meta convertToNormalMap:1; Trilinear filtering (C1-m3). Deferred if visual check still short: drop 30% albedo floor (B-channel dusty shift); port LiverTissueGPU procedural micro-mottle/vein noise (kills 10x blotch repetition). USER: reopen Unity (textures re-import), just Play — no inspector changes needed (0.07 migrates automatically).
- 2026-07-02 (k): **Render correction v3+v3.1 — the TRUE reference look identified and ported; verify wf_35f93c51 PASS.** Extraction wf_7e66fcc2: visible reference liver = Project2_Liver2 URP/Lit (BaseColor 0.7205882 gray x liver2 tiling(2,2), Metallic 0.184, Smoothness 0.939; LiverTissueGPU tet surface HIDDEN by default, SoftBody.cs:917; scene directional light intensity 2.0). v3: NEW _EnvReflStrength env-specular term in LiverSurface.shader (skybox probe, the wet sheen); pack Roughness 0.061/TextureStrength 1/Fresnel 0.1/SSS off/EnvRefl 1; triplanarScale 0.112 (=2/17.8; migrates 0.07 AND 0.55); matchReferenceLighting=true -> directional 2.0. v3.1 (review wf_57622e7c BLOCKER): Color props are sRGB->linear converted at bind — the /3.5+x3.5 split did not commute (albedo 3.9x dark = the dark render). Conversion-proof law: albedo=_Color*tex (x3.5 + 30% floor DELETED), _Color=0.7205882 (reference BaseColor, GPU-exact), _SpecularColor (0.338,0.207,0.178) = exact F0 (0.059,0.038,0.035). Deviations kept: real bump, no parallax/offset. USER: reopen Unity, Play (0.07/0.55 migrate automatically), visual check vs reference screenshot.
- 2026-07-03: **Haptic rendering (paper SS2.3 SDF Finger-Proxy) IMPLEMENTED — collision detection + grasping + friction as the single SS2.3 algorithm. Plan 2026-07-03-haptic-rendering-plan.md (5 review rounds all approve incl. a paper line-by-line/formula-by-formula audit wf_ec4f7646); implemented in 4 stages, each multi-subagent reviewed (A wf_f4ad0067, B wf_c2b24175, C wf_ea852ae5 — all PASS).** New native module cuda/haptics.cu + haptics.h: Eq 26 alternating projection on the capsule AXIS (k_ClosestPerTriangle, one thread/triangle, skin-only via bit30 aux flag), Algorithm 1 outer loop (min-||D|| among PENETRATING triangles per line 16; GPU argmin k_ReduceVmsKey/k_PickVmsTri on the non-negative key; P_t += a*v_ms, a=0.1, eps=1e-3, i<100; outerMax cap D10), Eq 22-25 proxy-vs-physical deviation (dP/dQ/theta/axisK, quatMul/quatConj), Eq 27 force+torque (F=Kp*dP-Dp*v, tau=Ko*theta*k-Do*w, damping contact-gated), thresholded Newton reaction scattered onto the min-triangle's 3 nearest ACTIVE corners via atomicAdd (k_ScatterReaction). Friction = the implicit tangential Kp*dP of the finger-proxy (NO Coulomb term — the paper has none); grasping = the SAME reaction mechanism (Fig 3.11/3.12, no separate algorithm). physics.cu: new d_hapticForce[cornerCount] added into k_ComputeSlope's slope (extForce+hapticForce+Fint)/mass; zeroed by haptics_step NOT physics_step (cross-frame BLOCKER guard). dc_recon: recon_expand_pos/recon_expand_aux getters (skin/wall soup). plugin_api: 6 LCS_*Haptic* exports. C#: HapticTool.cs P/Invoke driver (finite-diff v/omega, proxy render, HUD) + LiverCudaManager wiring (init after cut, step after LCS_Finalize, IJKL/UO keyboard capsule, proxy LineRenderer, |F|/|tau|/contact HUD, shutdown). All native compute in C++/CUDA, C# only I/O+render (SS3.1). Deviations D10-D15 ledgered (all faithful readings of the paper's own algorithm scope). CPU oracle cuda_plugin/tests/haptics_oracle.py 12/12 PASS (SDF/Eq26/Algorithm1/Eq22-27/torque behavioral + presence gates). DLL md5 20158ff1ab7191f56f5ee5b1f326b2b7 — USER: close Unity, copy build/Release/LiverCudaSim.dll -> Assets/Plugins/x86_64, reopen (C# recompiles HapticTool), Play; drive the haptic capsule with IJKL/UO into the liver -> HUD |F| rises on contact, proxy stays on the surface (no penetration), tissue dents under the proxy; releasing drops |F| to 0.
- 2026-07-03 (b): **Haptic bugs 1-3 fixed (no-deformation / Fig 3.1 architecture / Haply Inverse3 device) — full mandated per-bug process: diagnosis wf_c7c6efc2 (D1/D2/D3) -> 3 fixes committed -> per-bug review wf_4941bdfd (R1/R2 each 1 major) -> re-fix -> re-review wf_7f5d7173 (R1 PASS, R2 confirmed a regression) -> re-fix -> round-3 review wf_ea6f2eb7 (R1+R2 BOTH PASS high, 0 blocker/major, adversarial verify empty).** BUG 1 (contact made no visible deformation): diagnosis proved NO pipeline bug — purely quantitative (12.5N / 3 corners vs ks=1e4 -> dent ~2e-4 L, 3 orders below visible) AND the paper's actual mechanism is SS3.2 line 985-987 "applied to UPDATE THE PARTICLE POSITIONS" over the plural "nearby triangle faces" (SS2.3 917-919). FIX: contact-patch kernels replacing k_ScatterReaction — k_CountPenetrating (atomicAdd count + depthSum), k_ScatterContact (per (triangle,vertex-lane): depth-weighted thresholded force + beta*depth position displacement onto the nearest DEFORMABLE corner), k_ApplyContactDisp (clamp ||d|| to dispMax=0.15L/frame, cornerPos+=d, kill the opposing spring-back velocity). CRITICAL subtlety: the reaction block RE-RUNS k_ClosestPerTriangle at the PHYSICAL tool pose (g_physS/E) before scattering — the converged proxy has depth~0, so the dent must track the physical penetration. Tunables exposed (hapticKp/Dp/ReactionMax/ContactBeta 0.6/DispMaxOverL 0.15). physics_vel() getter added. **Review R1 major (round 2): pinned corners are `active`, so the NEW position path displaced the anchored BC (k_IntegrateY5 freezes+never restores) -> touching the cap dented it permanently.** RE-FIX: physics_pinned() getter; k_ScatterContact skips pinned in the nearest-corner search (routes dent to nearest deformable, D11 intent) + k_ApplyContactDisp early-outs on pinned (defense-in-depth); oracle test_pinned_corner_protection (14/14). BUG 2 (Fig 3.1 Computing framework): box-by-box audit found the DATAFLOW already IDENTICAL ("Move Proxy to the Physics Tool Position" = per-frame proxy re-seed D13; "Add Tool Force" = d_hapticForce in k_ComputeSlope); the only gap is the 3-thread FORM. Adjudicated in ledger D16 — the paper's own Haptic Thread was OpenHaptics' servo loop (SS3.1); ours = the Haply SDK's 1-8 kHz device thread doing zero-order-hold force replay, sim-side proxy solve at frame rate (residual = proxy/force SOLVE RATE, disclosed; future ~1 kHz sim thread closes it). D17 = the dual force+position reaction. BUG 3 (Haply Inverse3 device): research (develop.haply.co / docs.haply.co) -> Inverse Service (Windows, WebSocket) + Unity package co.haply.inverse (Inverse3 class, DeviceStateChanged 1-8 kHz, CursorSetLocalForce Newtons). VERDICT: device layer = C# (paper SS3.1 renders/IO on the Unity side via the OpenHaptics Unity plugin; all force compute stays native C++/CUDA). New Inverse3Driver.cs: #if HAPLY_INVERSE (auto-defined via asmdef versionDefines on co.haply.inverse), allocation-free DeviceStateChanged callback (pose in / ZOH force out), simToNewton 0.2 + 3N soft/8N hard cap + 0.5s ramp + zero on disable/quit; keyboard fallback when the define/device is absent. LiverCudaManager: device pose replaces IJKL/UO when active, SetSimForce after each Step. **Review R2 major (round 2): IsActive=true set on component-found with no readiness check -> service-down froze the tool. Fixed with IsReady gate; but re-review confirmed the fix's `IsReady || _gotCallback` MASKED IsReady after the first callback latched -> a SILENT disconnect (no OnDisable) froze the tool at the stale pose.** FINAL FIX: `ready = IsReady && callbacksFresh` where callbacksFresh = a volatile _stateSeq bumped per device tick, stale after deviceStaleTimeout=0.2s — the only signal surviving a silent unplug, and necessary+sufficient for a live pose; any device-down state returns control to the keyboard within 0.2s even if IsReady is stuck true. Minors also fixed: stale k_ScatterReaction header comment, dispMax 0.25L->0.15L, D16 solve-rate disclosure, c_pinned=nullptr in shutdown. Oracle 14/14 PASS, build clean. Commits 7937e21/564820c (initial), bb8fe62 (R1+R2 round-2 fixes), 1b0cbd9 (R2 disconnect final). FINAL DLL md5 f59664956e4cf2855e63230fa6ab315d — USER: close Unity, copy build/Release/LiverCudaSim.dll -> Assets/Plugins/x86_64, reopen, Play; press the capsule into the liver -> visible dent that tracks penetration depth (grows with push, springs back on release); optional Haply Inverse3: install Inverse Service >=3.1 + co.haply.inverse package (scoped registry https://unitypackages.haply.co, scope co.haply) -> HAPLY_INVERSE auto-defines -> add the Haptic Rig, feel |F| on the device (0.2 N/sim unit, 3N cap, 0.5s ramp).
- 2026-07-04: **Bug 1 REAL fix — the tool was still PENETRATING the liver (穿模) with tiny force + CONTACT/free flicker. Root-caused to the MISSING persistent finger-proxy and fixed (Task A1). Deterministic repro + 4-agent diagnosis wf_de18f2f3 -> plan -> plan-review wncvquq3j (3 blockers/majors incorporated) -> impl -> A1 review wur30rv7x (1 major) -> fix -> A1 re-review wroqdtb9s (source fix CONFIRMED closed; sole blocker = DLL deploy).** The prior "dent" work (2026-07-03 b) treated a symptom; the user's real complaint = the paper's §2.3 Finger-Proxy (Chai3D god-object) guarantees the tool NEVER penetrates (proxy stays on the surface, physical goes in, spring connects). DIAGNOSIS (repro scratchpad/proxy_tunnel_repro.py, reproduced old tool tunneling to y=-4.9 / F->0 / 2 flips): TWO independent tunneling causes — (1) the per-frame proxy RE-SEED to the physical pose (old ledger D13, haptics.cu:299-302) destroyed the persistent P_t Algorithm 1 requires; (2) the FREE flag key=(axisDist<radius)?depth:1e30 (haptics.cu:137) read no-penetration once the tool was deeper than one radius, so a fast plunge free-stepped through. The inner alternating projection (Eq 26) was FAITHFUL and kept. FIX A1 (paper §2.3 persistent god-object): persistent proxy carried frame-to-frame (g_hasProxy), re-projected onto the freshly-rebuilt soup each frame (re-anchor; NO triangle indices); state machine FREE->proxy≡physical (dP=0, no phantom force) / rising edge -> NEAR-SIDE seed at g_prevPhysPos (outside) + separate / IN CONTACT -> bounded attraction proxyTrackStep=0.5L (D19, §2.3 prose "approaches in discrete steps", ABSENT from Algorithm 1) then Algorithm 1 separation / exit when dP->0; detection at the physical pose is ONLY the rising-edge gate so a DEEP physical does NOT drop contact (persistence, not re-detection). captureMargin is a PER-CALL kernel arg (D18): the detection pass widens to max(0.25L, |physPos-prevPhysPos|) so a fast plunge is captured, while the separation loop AND the D17 dent pass pass margin 0 (true radius) — no leak. Damping/force predicate decoupled: g_inContact = g_hasProxy && |dP|>eps ("damping only during contact", line 929). Interior-side outward-sign guard (D15): reorient Pi-A to the face normal only when it points inward. Native contact export LCS_GetHapticContact; C# InContact reads it (not |F|>eps). ABI HapticParams 11f+1i (added captureMargin, proxyTrackStep). Ledger D13 REWRITTEN, D14/D16 restated, D15 extended, D18/D19 added. A1 REVIEW (wur30rv7x) caught a real major: the rising-edge/lost-support seed placed the proxy AT the (possibly >1-radius-deep) physical -> separation finds no penetrating tri -> stranded interior -> tunnel; the oracle 1-D model masked it. RE-FIX (fcdd1de): seed at g_prevPhysPos (provably outside on the rising edge) + lost-support re-seed to the saved pre-attraction surface pose; captureMargin base 1.5L->0.25L; oracle now models the beyond-radius "cannot escape" case (py<=-r stuck) so test_persistent_proxy_no_tunnel is LOAD-BEARING (old seed-at-physical makes it fail). A1 RE-REVIEW (wroqdtb9s): both lenses CONFIRM the source fix closes the >1-radius tunnel by induction; sole remaining blocker = the fixed DLL was never deployed (Unity locks Assets/Plugins/x86_64/LiverCudaSim.dll; I can't overwrite it while the editor is open). Added a NON-FATAL CMake POST_BUILD auto-deploy (deploy.cmake) so source/binary can't silently diverge again. Oracle 15/15 PASS, build clean. Commits acf6d55 (A1) / fcdd1de (near-side seed) / 3a?(auto-deploy). BUILT DLL md5 c7265f115d3e34e842fb6653b0b3b09e — USER: CLOSE Unity (releases the lock), then either re-run `cmake --build build --config Release` (auto-deploys) OR copy cuda_plugin/build/Release/LiverCudaSim.dll -> Assets/Plugins/x86_64/, reopen, Play; press the小棍 into the liver -> it STOPS AT THE SURFACE (never penetrates), |F| grows with push depth, no flicker, release restores. STILL TODO this feature: Task B (assemble the 3-part grasper mesh as the on-surface PROXY + a ghost PHYSICAL tool + spring line) and Task C (open/close jaws + grasping hold-constraint) — both queued, each multi-subagent reviewed.
- 2026-07-04 (b): **穿模 CONFIRMED FIXED in-app (user screenshot: orange proxy on surface, red physical inside, |dP|=0.67, CONTACT, |F|=534) — but the earlier signed-test story had a middle chapter: the persistent-proxy fix was DEPLOYED yet still tunneled, root-caused (multi-agent wik7z4vrv, numerically verified) to TWO more causes and fixed with the SIGNED constraint-plane test.** The unsigned axisDist<radius test (old D15) had NO inside/outside notion, so a tool >1 radius deep read "not penetrating" -> proxy stranded deep + HUD "free" (MODE 1); and outerMax=64 was one iter short of climbing a radius at a=0.1 (MODE 3). FIX (d49b36e): signedDist=dot(Pi-A, faceNormal); depth=radius-signedDist (>radius once inside); penetrating iff signedDist<radius; push along the face normal — the classic god-object constraint half-space + tangential sliding; outerMax 64->128. Review w4eyhuj28 caught 2 majors -> fixed (458ac16): select by the paper's ||D||=||Pi-A|| not the plane offset (a far tangent face could win); sign fn against the recon vertex normal recon_expand_nrm() (winding guard, load-bearing invariant D15). Then the FEATURE turn: **friction + grasp + grasper mesh + Haply + bigger deformation** (research weo6zf2bt: Chai3D cAlgorithmFingerProxy + paper §2.3 + Haply path). FRICTION (root cause: proxy attracted straight to physical -> dP normal-only -> slides freely; the paper §2.3 has NO explicit friction term, friction = implicit tangential Kp*dP the paper assumes but never spells out): added the Chai3D friction cone — a persistent world stick anchor (g_stickAnchor) held tangentially until the pull exceeds frictionRadius=normalDepth*mu, then slips at kinetic mu with hysteresis; Eq 27 untouched, dP now carries the tangential residual = friction (D20). GRASP (D21): hold G -> haptics_set_grasp scales mu 6x -> grip/drag. GRASPER MESH: GrasperRig.cs loads the 3 OBJs -> proxy steel + physical red ghost + spring, replaces the two lines; jaws animate. HAPLY (user connected the device): the 3N cap saturated regardless of press depth (534*0.2=107->cap 3) -> simToNewton 0.2->0.012 + softCap 3->6 so a firm press feels ~6N. DEFORM (dent capped at dispMax=0.15L=0.056u/frame) -> dispMax 0.45L + beta 0.85 + reactionMax 200 + Kp 1400. Review w5i8uv8tw caught a BLOCKER: the committed deployed DLL was STALE (11-float HapticParams) vs source (13-float, muS/muK) -> C# 56B struct into 48B DLL corrupts outerMax=muS=0.6f -> 1.06e9-iter GPU HANG; EntryPointNotFound guard misses struct-size drift. FIX (ec45577): LCS_HapticAbiVersion()=sizeof(HapticParams); HapticTool.Init size-checks BEFORE Init -> stale DLL disables gracefully, never hangs; DLL rebuilt+auto-deployed (build md5==deployed md5). Friction depth now live proxy-vs-physical (no grip creep); GrasperRig transparent ghost + null-safe + Build try/catch. Re-review wkhk60t0s PASS (0 blocker/major, adversarial verify empty). Oracle 16/16 (new test_friction_cone: stick/slip/Coulomb/grasp). FINAL DLL md5 e118e8c2e24077bdb919ed606b8711d2 (deployed). USER: reopen Unity, Play; press liver -> big dent + ~6N on the Haply; drag -> friction (no slide); hold G -> grip/drag; grasper mesh instead of rods. Known minors ledgered: friction anchor world-fixed (tangential material-tracking future), g_lastNormal/depth 1-frame lag (bounded).
- 2026-07-04 (c): **Violent haptic OSCILLATION (|F|=6795 N, |dP|=8.49u, tool latched 10u BELOW the liver) + no-visible-deformation FIXED; grasper shrunk; friction raised. Diagnosis wpoirpnc5 (numerically confirmed) -> fix -> review w13ag9ld8 (1 blocker) -> fix -> re-review wtij2sh5k (2 majors) -> fix -> re-review wib19l9ks PASS (0 issues).** ROOT CAUSES (ranked): #0 the haptic collision capsule reused rodLength=12 (a 12u ROD) -> the nearest-axis point slid along its length -> |dP| grew to several units; FIX = dedicated SHORT hapticProbeLength=1.2u. #1 (dominant) the exit-to-free test was |proxy-phys|<eps, which friction keeps large, so a tool dragged far or plunged THROUGH the liver stayed latched g_hasProxy with a ~10u phantom dP -> 6795 N forever AND the dent pass then found no near penetrating tri -> "no deformation". #2 frictionRadius=depth*mu grew unbounded with depth -> superhuman deep grip inflating dP; FIX = cappedDepth=min(depth, toolRadius). #3 the stiff Ks=1e4 spring over-restored the position dent and rebounded it back THROUGH the surface at ~14 u/s -> the jitter; FIX = k_ApplyContactDisp strongly damps the whole contact-corner velocity (vel*=0.15) so the dent HOLDS (Ks unchanged — softening can't help, the force path is capped by reactionMax; the visible dent is the position update). + proxy attraction 0.5 relaxation, hapticDp 5->8. The EXIT (#1) needed 3 rounds: (a) re-detect physical penetration instead of |dP|<eps — but haptics_detect used the SIGNED infinite-plane per-face test, which on a CLOSED body reports "penetrating" almost everywhere (a far face is always behind its plane), so physInside was always true (review w13ag9ld8 blocker); (b) FIX = k_NearestSigned + haptics_phys_inside: find the geometrically NEAREST skin face (min ||Pi-A||) and release iff its signedDist >= radius — a proper point-in-body test (deep press holds at any depth; pass-through/withdraw/side-drag release). The SAME over-report also (c) starved the dent (scattered onto the far side) -> proximity-gate the dent on ||Pi-A||; and (d) leaked into the ENTRY path (hover-above false rising edge -> phantom buzz, review wtij2sh5k MAJOR 1 -> haptics_detect returns wk<radius+margin) and (e) the fixed dent cutoff gave zero deform past ~1.1*radius (MAJOR 2 -> adaptive proximityMax = minDist + radius + margin so the entry patch dents at ANY depth while the far side is excluded). Also: grasper mesh shrunk to match the short probe (user: too big); friction muS 0.6->0.8/muK 0.45->0.65 (user: more grip, safe now the cone is capped). Oracle 17/17 (new test_physinside_closed_body: slab top+bottom faces — nearest-face releases pass-through, hover-above not-detected, entry patch dents at depth 0.3/3/8, far side excluded; the flat 1-D model could NOT express these closed-body pathologies). Commits 048cf08 (osc+probe+friction) / 671831b (nearest-face exit + proximity dent) / 6f295ac (entry proximity + adaptive dent). FINAL DLL md5 7a59aa471b50fb8cc67ed6e032bbbd19 (deployed). USER: reopen Unity, Play; press liver -> clean DENT (shallow+deep), |F| tens-to-hundreds N (not thousands), |dP| < ~2u; drag -> friction; plunge THROUGH -> auto-release (no phantom latch); hover above -> no buzz; grasper smaller.
- 2026-07-04 (d): **BUG 1 (liver won't locally deform even after Ks lowered 10x) + BUG 2 (make the grasper collision box precise — use the simplified jaw MESHES directly for collision, not capsules; fine model display-only) FIXED. systematic-debugging diagnosis -> fix -> reviews wysyvnnmd (1 blocker + 1 major) -> fix (46fbb40) -> re-review widyj4a0t (PASS, high conf, adversarial verify empty) -> oracle hardening (c8df54b).** BUG 1 root cause (NOT a bug, quantitative + wrong knob): the visible dent is the **Ks-INDEPENDENT position update** (paper SS3.2 "reaction threshold -> update particle positions"), throttled by dispMax; the FORCE path is spring-capped (reactionMax) so lowering Ks does nothing to the dent AND makes gravity-mode too soft (the tension the user hit). FIX = keep Ks HIGH (user restores 10000 for gravity), raise hapticDispMaxOverL 0.45->2.0 so the dent scales with penetration depth (up to ~1 radius/frame) -> a clearly visible depth-scaling dent (committed 512a5c2, reviewed PASS). BUG 2 (direct mesh collision, user verbatim "直接使用这个简化模型直接与肝脏之间进行碰撞计算，不使用多个胶囊"): the tool is now the low-poly JAW COLLISION MESHES (Haptic_grasper_jaws_up/down_collision.obj), not a capsule. cuda/haptics.cu: ClosestOnToolMesh(toolTri,count,shift,p)=min over tool tris of ClosestOnTriangle(shifted); ClosestOnTool dispatches mesh-or-capsule (toolCount>0 ? mesh : segment); k_ClosestPerTriangle/k_NearestSigned take d_toolTri/g_toolTriCount/toolShift and use it for P0 + both Eq-26 half-steps + the nearest-face exit. Small SKIN radius (hapticSkinRadius=0.2u Minkowski) keeps the thin jaws watertight vs the coarse tissue voxel L~0.37u. haptics_set_tool_mesh uploads world verts (host centroid+bounding-radius g_toolCenter/g_toolBoundR); proxy tool mesh = physical-pose mesh translated by proxy-phys (pshift). GrasperRig.CollisionSoupWorld builds the world jaw-tri soup each frame (root rot * (hinge Euler(+-open)*localVert + hingeLocal)); LiverCudaManager uploads it via LCS_SetToolMesh before Step, radius = hapticSkinRadius when mesh present else capsule. **Review wysyvnnmd BLOCKER: the committed deployed DLL lacked LCS_SetToolMesh -> the mesh path was silently inert (capsule fallback).** FIX = rebuild + deploy the fresh DLL (Unity closed so cmake auto-deploy AND explicit cp both landed; deployed md5==build md5, byte-verified to export LCS_SetToolMesh) + harden HapticTool.SetToolMesh to LogWarning ONCE on EntryPointNotFound (not a silent catch). **Review wysyvnnmd MAJOR: the AABB-lite cull used only the tissue-tri CENTROID -> a straddling tri (centroid just outside, a vertex penetrating at the jaw tip) was false-culled -> jaw-tip tunnel.** FIX = add the tissue-tri circumradius to the cull threshold: triR=max(|a-ctr|,|b-ctr|,|c-ctr|); cull iff |ctr-toolQueryCenter| > boundR+radius+captureMargin+triR (proven gap-free by re-review widyj4a0t: any tri with a penetrating point is retained; strict '>' keeps boundary tris; ~30% wider threshold, still culls the vast majority). Re-review minor closed (c8df54b): oracle was source-text only so it could not catch a STALE deployed binary (the exact blocker blind spot) -> new test_deployed_dll_exports ctypes-LOADS Assets/Plugins/x86_64/LiverCudaSim.dll and asserts the full haptic export set (skips gracefully if absent) + cull-gate assertion tightened to include triR. Oracle 18/18 PASS (incl. live DLL load). Commits 512a5c2 (dispMax) / 46fbb40 (cull triR + fresh deploy + warn-once) / c8df54b (oracle live-DLL + cull gate). DEPLOYED DLL md5 36aced59c40d3092574417a6a1ca237d (exports LCS_SetToolMesh, load-verified). USER: reopen Unity, set Ks back to 10000 (gravity normal; the dent is Ks-independent), Play; press the liver -> a clear dent that GROWS with push depth; collision now uses the real FLAT JAW SHAPE (not a round capsule) so the contact box is precise; hapticSkinRadius(0.2)/hapticDispMaxOverL(2.0) tunable in the Inspector.
- 2026-07-04 (e): **Contact-algorithm rewrite — the 3 reported bugs (BUG1 proxy/physical coincide -> no more force [biggest]; BUG2 tool still tunnels [穿模]; BUG3 almost no deformation) were ONE shared root cause: my non-paper D19/|dP| machinery violated the paper's PERSISTENT god-object.** systematic-debugging + a 12-agent diagnosis (wf_395c900a: 3 independent paper-Algorithm-1 derivations ALL high-conf `tracks_toward_physical=false` + per-bug root cause + adversarial verify) proved: the paper's Algorithm 1 (lines 864-914) is a persistent god-object that ONLY pushes the proxy outward until on the surface and releases ONLY when the physical leaves the body (prose 915-916) — it has NO |dP| release, NO attract-toward-physical step, NO friction-cone. My haptics.cu had a non-paper exit disjunct `if(!physInside || |proxyPos-physPos|<eps)` that released the god-object whenever |dP| momentarily collapsed WHILE the physical was still buried (a straight deep press makes dP nearly pure-normal and small; the D19 attraction can over-track the proxy onto the physical). Consequences, all from that one line: (BUG1) release snaps the proxy onto the DEEP physical -> dP=0 => F=0 forever, and FREE's haptics_detect (wk<radius+margin) can't re-detect a >1-radius-deep physical -> permanent no-interaction; (BUG2) the DISPLAYED proxy (grasper mesh follows ProxyPos) is snapped onto the deep physical = visible tunneling; (BUG3) the dent pass, gated on `g_inContact = hasProxy && |dP|>eps`, never runs after the collapse. KEY INSIGHT (resolved a red herring): HUD screenshot A (proxyY=6.61 on-surface, physY=2.27 deep, |dP|=4.34 normal, |F|~3741) is CORRECT deep-press god-object behavior, not a bug — a deep physical with the proxy on the surface SHOULD give a large normal dP + force (a real Haply device would push the hand back); the tunneling was the release-snap, not the depth. The initial BUG2 fn*depth "walks along one fixed normal for 128 iters" hypothesis was REFUTED by adversarial verify (0/2) — the separation re-evaluates v_ms each outer iteration and converges — and re-attributed to the same release machinery. FIX (haptics.cu, commit 081028f): (1) exit ~:609 `if(!physInside)` — delete the |dP|<eps disjunct; release on the physical-inside test ALONE; when |dP| collapses while physInside, KEEP the proxy (haptics_separate already re-pinned it) so dP re-inflates to the true depth; (2) contact predicate ~:628 `g_inContact = g_hasProxy` (persistent proxy = physical penetrating), not `hasProxy && |dP|>eps` — F=Kp*dP is naturally 0 at dP=0 so no phantom force, and the dent re-evaluates true penetration at the physical pose independent of |dP|. Friction cone + D19 attraction UNCHANGED (kept the friction feature; the |dP| release was the culprit, not the friction which stays bounded). Reference repo D:\Download\recon-grid-dc has NO haptics module (only dc_recon/physics/plugin_api); its k_SetToolForce (distributed w=1-d/r force added to extForce, solver-driven) is a cleaner deformation-coupling idea noted for future but not adopted (paper uses the SS3.2 position update, which we keep). TDD: new oracle test_godobject_persistent_no_dp_release (deep static press never releases; transient dP->0 while inside does not release; genuine exit releases; source-gates the removed |dP| clauses). Oracle 19/19 PASS. Multi-subagent review wnhcw1x0x (BUG1/BUG2/BUG3/REGRESSION lenses + adversarial verify): ALL FIXED high-conf, 0 blocker/major, adversarial verify empty; 2 BUG2 PRE-EXISTING minors ledgered (nearest-face phys_inside convexity assumption on thin/non-convex regions -> possible momentary tunnel flash; fast-plunge double-false separation -> 1-frame surface lag) — deferred (evidence-driven; harden phys_inside with a winding-number inside test only if observed). BUILD md5 2d0950a106bd24502e41d5669ae1c4bf (has LCS_SetToolMesh); DEPLOY PENDING (Unity was open, lock held). USER: CLOSE Unity, copy cuda_plugin/build/Release/LiverCudaSim.dll -> Assets/Plugins/x86_64/ (or re-run cmake --build build --config Release which auto-deploys), reopen, Play; press the liver -> the steel PROXY tool STAYS ON THE SURFACE (no tunnel), |F| persists as you hold/press deeper (no dropout on coincidence), and a visible depth-scaled DENT appears every contact frame; the red PHYSICAL ghost showing raw device depth is intentional (what a real device pushes your hand back from).
- 2026-07-04 (f): **Deformation/force pair — BUG-A (thin precise grasper pokes a BIG CRATER) + BUG-B (gravity + Ks=10 super-soft -> springs tear; user wants stronger instrument force to squeeze). Diagnosis wf_6703c058 (2 bugs, adversarial verify), fixes reviewed (BUG-A w03gm76s4, BUG-B w0zn7bikz) — all PASS, deployed.** ROOT CAUSE (both confirmed high-conf): (BUG-A) the position dent was a PLASTIC ACCUMULATING write, not the paper SS3.2 BOUNDED non-penetration constraint — depth = radius - signedDist is UNBOUNDED (grows past the radius as the physical axis goes deeper), scaled by contactBeta=0.85 and clamped only by dispMax=2.0*L=0.74u/frame, written into cornerPos with recovery killed (vel*=0.15); a deep press punched a single-frame hole and, with the recon-rebuild feedback + soft Ks, carved a crater far deeper/wider than the thin jaw. HUD |dP|=0.29 (small, correct god-object) vs the big crater proved the crater is this SEPARATE dent path, not dP. (BUG-B) the FORCE path is the only Ks-aware (elastic) deformation channel and is throttled by reactionMax=200 AND the intrinsic Ks stiffness (the code's own note: ~1000N -> ~0.002u at Ks=7.5e4 — a PHYSICS FLOOR, not a bug), so force alone cannot visibly squeeze a gravity-safe stiff liver; the user lowered Ks to 10 to force deformation, pushing gravity strain past the tear threshold (k_TearOverstretch severs any edge > tau*L, tau=4.0, global, gated on g_toolSet) -> disintegration. KEY INSIGHT: the VISIBLE deformation should be the Ks-INDEPENDENT bounded dent (works at any Ks, recovers elastically), the FORCE path is for haptic feel — so keep Ks gravity-safe and let the bounded dent deform. FIX BUG-A (haptics.cu 869cd4d): dentDepth = fminf(depth, dentDepthMax) with dentDepthMax = g_toolRadius (position push bounded to one tool radius; the surface converges to the tool penetration WITHOUT single-frame overshoot; the FORCE weighting keeps the true depth, capped separately by reactionMax); dent footprint band tightened proximityMax = minDist + g_toolRadius (dropped the captureMargin widening — a fast-plunge DETECTION term that must not spread the crater); hapticDispMaxOverL 2.0 -> 0.6 (finer step). Coarse voxel L~0.37 is a HARD FLOOR on dent width. FIX BUG-B (LiverCudaManager.cs eb81139, user chose the SAFE option — do NOT touch the tear law -> zero cutting risk): Kp 1400 -> 2200 (Eq22 inter-instrument spring), reactionMax 200 -> 600 (tissue reaction cap), Dp 8 -> 10 (stability). Keep Ks GRAVITY-SAFE (default ks=7.5e4 sags only 2.3x, well under tau=4.0; do NOT use 10). Review findings (both PASS, 0 blocker/major, adversarial verify empty): reactionMax=600 is a POSITION-INDEPENDENT constant forcing term (does not feed the stiffness Jacobian -> cannot cause RK explosion; at gravity-safe Ks the lattice is ~10x more stable than the hMax=1.5e-3 calibration point); the prior wpoirpnc5 oscillation was phantom-dP/exit-latch/friction, all fixed, NOT the reaction cap. MINORS (guidance, no code change): tau (tearStretchRatio) is ALREADY a public Inspector field (default 4.0) with headroom over the 2.3x gravity strain, so a MIDDLE Ks (3e4-5e4) + slightly raised tau is a self-serve softer-liver path with no cutting risk; DENT SIZE is tuned by hapticContactBeta + hapticDispMaxOverL (NOT Kp/reactionMax). TDD: new test_bounded_dent_no_crater (bounded dent conforms to the tool without overshoot; per-frame step <= beta*radius; old unbounded law punches past). Oracle 20/20 PASS (incl. live deployed-DLL load). DEPLOYED md5 61e85ae44f5c8e38f62a4a9bd801271e (Unity released the lock -> deployed + committed 92ff140; has LCS_SetToolMesh). USER: Play with the new DLL; set Ks back to a GRAVITY-SAFE value (~10000-75000, NOT 10) + gravity ON -> no tear; press the liver -> a clean TOOL-SHAPED dent (no big crater) that recovers on release; stronger device force/feel; for a bigger dent tune hapticContactBeta/hapticDispMaxOverL; for a softer liver raise tearStretchRatio and lower Ks together.
